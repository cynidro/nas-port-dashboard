services:
  port-dashboard:
    build: .
    container_name: port-dashboard
    restart: unless-stopped
    ports:
      - "3001:3001"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      - PORT=3001
      - TZ=Asia/Seoul
